DATABASE_URL=postgresql://kurultay:kurultay@localhost:5432/kurultay
REDIS_URL=redis://localhost:6379
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
API_PORT=4000
WEB_URL=http://localhost:3000
# Baked into the web image at `docker compose build` time (Next.js inlines NEXT_PUBLIC_*
# at build, not at container start) — rebuild the web image after changing this.
NEXT_PUBLIC_API_URL=http://localhost:4000
